/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';
import { expectAddress, expectSome, getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_CLAIM_ROOT_DISCRIMINATOR = new Uint8Array([223, 19, 114, 87, 232, 189, 8, 164]);

export function getSetClaimRootDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_CLAIM_ROOT_DISCRIMINATOR);
}

export type SetClaimRootInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountClaimRoot extends string | IAccountMeta<string> = string,
  TAccountDistributor extends string | IAccountMeta<string> = string,
  TAccountBase extends string | IAccountMeta<string> = string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> = '11111111111111111111111111111111',
  TAccountTokenProgram extends
    | string
    | IAccountMeta<string> = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountClaimRoot extends string ? WritableAccount<TAccountClaimRoot> : TAccountClaimRoot,
      TAccountDistributor extends string
        ? ReadonlyAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountBase extends string
        ? ReadonlySignerAccount<TAccountBase> & IAccountSignerMeta<TAccountBase>
        : TAccountBase,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & IAccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetClaimRootInstructionData = {
  discriminator: ReadonlyUint8Array;
  root: Array<number>;
  configurator: Array<number>;
};

export type SetClaimRootInstructionDataArgs = {
  root: Array<number>;
  configurator: Array<number>;
};

export function getSetClaimRootInstructionDataEncoder(): Encoder<SetClaimRootInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['root', getArrayEncoder(getU8Encoder(), { size: 32 })],
      ['configurator', getArrayEncoder(getU8Encoder(), { size: 32 })],
    ]),
    (value) => ({ ...value, discriminator: SET_CLAIM_ROOT_DISCRIMINATOR })
  );
}

export function getSetClaimRootInstructionDataDecoder(): Decoder<SetClaimRootInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['root', getArrayDecoder(getU8Decoder(), { size: 32 })],
    ['configurator', getArrayDecoder(getU8Decoder(), { size: 32 })],
  ]);
}

export function getSetClaimRootInstructionDataCodec(): Codec<
  SetClaimRootInstructionDataArgs,
  SetClaimRootInstructionData
> {
  return combineCodec(
    getSetClaimRootInstructionDataEncoder(),
    getSetClaimRootInstructionDataDecoder()
  );
}

export type SetClaimRootAsyncInput<
  TAccountClaimRoot extends string = string,
  TAccountDistributor extends string = string,
  TAccountBase extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  claimRoot?: Address<TAccountClaimRoot>;
  distributor?: Address<TAccountDistributor>;
  base: TransactionSigner<TAccountBase>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  root: SetClaimRootInstructionDataArgs['root'];
  configurator: SetClaimRootInstructionDataArgs['configurator'];
};

export async function getSetClaimRootInstructionAsync<
  TAccountClaimRoot extends string,
  TAccountDistributor extends string,
  TAccountBase extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: SetClaimRootAsyncInput<
    TAccountClaimRoot,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SetClaimRootInstruction<
    TProgramAddress,
    TAccountClaimRoot,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    claimRoot: { value: input.claimRoot ?? null, isWritable: true },
    distributor: { value: input.distributor ?? null, isWritable: false },
    base: { value: input.base ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            77, 101, 114, 107, 108, 101, 68, 105, 115, 116, 114, 105, 98, 117, 116, 111, 114,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.base.value)),
      ],
    });
  }
  if (!accounts.claimRoot.value) {
    accounts.claimRoot.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([67, 108, 97, 105, 109, 82, 111, 111, 116])),
        getAddressEncoder().encode(expectAddress(accounts.distributor.value)),
        getArrayEncoder(getU8Encoder(), { size: 32 }).encode(expectSome(args.root)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.claimRoot),
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.base),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.tokenProgram),
    ],
    programAddress,
    data: getSetClaimRootInstructionDataEncoder().encode(args as SetClaimRootInstructionDataArgs),
  } as SetClaimRootInstruction<
    TProgramAddress,
    TAccountClaimRoot,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram,
    TAccountTokenProgram
  >;

  return instruction;
}

export type SetClaimRootInput<
  TAccountClaimRoot extends string = string,
  TAccountDistributor extends string = string,
  TAccountBase extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  claimRoot: Address<TAccountClaimRoot>;
  distributor: Address<TAccountDistributor>;
  base: TransactionSigner<TAccountBase>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  root: SetClaimRootInstructionDataArgs['root'];
  configurator: SetClaimRootInstructionDataArgs['configurator'];
};

export function getSetClaimRootInstruction<
  TAccountClaimRoot extends string,
  TAccountDistributor extends string,
  TAccountBase extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: SetClaimRootInput<
    TAccountClaimRoot,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SetClaimRootInstruction<
  TProgramAddress,
  TAccountClaimRoot,
  TAccountDistributor,
  TAccountBase,
  TAccountPayer,
  TAccountSystemProgram,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    claimRoot: { value: input.claimRoot ?? null, isWritable: true },
    distributor: { value: input.distributor ?? null, isWritable: false },
    base: { value: input.base ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.claimRoot),
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.base),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.tokenProgram),
    ],
    programAddress,
    data: getSetClaimRootInstructionDataEncoder().encode(args as SetClaimRootInstructionDataArgs),
  } as SetClaimRootInstruction<
    TProgramAddress,
    TAccountClaimRoot,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram,
    TAccountTokenProgram
  >;

  return instruction;
}

export type ParsedSetClaimRootInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    claimRoot: TAccountMetas[0];
    distributor: TAccountMetas[1];
    base: TAccountMetas[2];
    payer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
    tokenProgram: TAccountMetas[5];
  };
  data: SetClaimRootInstructionData;
};

export function parseSetClaimRootInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedSetClaimRootInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      claimRoot: getNextAccount(),
      distributor: getNextAccount(),
      base: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getSetClaimRootInstructionDataDecoder().decode(instruction.data),
  };
}
