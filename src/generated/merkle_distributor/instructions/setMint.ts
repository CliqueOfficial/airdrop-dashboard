/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';
import { expectAddress, getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_MINT_DISCRIMINATOR = new Uint8Array([166, 129, 167, 223, 137, 118, 212, 47]);

export function getSetMintDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_MINT_DISCRIMINATOR);
}

export type SetMintInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountDistributor extends string | IAccountMeta<string> = string,
  TAccountBase extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountDistributor extends string
        ? WritableAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountBase extends string
        ? ReadonlySignerAccount<TAccountBase> & IAccountSignerMeta<TAccountBase>
        : TAccountBase,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetMintInstructionData = {
  discriminator: ReadonlyUint8Array;
  mint: Address;
};

export type SetMintInstructionDataArgs = { mint: Address };

export function getSetMintInstructionDataEncoder(): Encoder<SetMintInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['mint', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MINT_DISCRIMINATOR })
  );
}

export function getSetMintInstructionDataDecoder(): Decoder<SetMintInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['mint', getAddressDecoder()],
  ]);
}

export function getSetMintInstructionDataCodec(): Codec<
  SetMintInstructionDataArgs,
  SetMintInstructionData
> {
  return combineCodec(getSetMintInstructionDataEncoder(), getSetMintInstructionDataDecoder());
}

export type SetMintAsyncInput<
  TAccountDistributor extends string = string,
  TAccountBase extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  distributor?: Address<TAccountDistributor>;
  base: TransactionSigner<TAccountBase>;
  systemProgram?: Address<TAccountSystemProgram>;
  mint: SetMintInstructionDataArgs['mint'];
};

export async function getSetMintInstructionAsync<
  TAccountDistributor extends string,
  TAccountBase extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: SetMintAsyncInput<TAccountDistributor, TAccountBase, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SetMintInstruction<TProgramAddress, TAccountDistributor, TAccountBase, TAccountSystemProgram>
> {
  // Program address.
  const programAddress = config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    base: { value: input.base ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            77, 101, 114, 107, 108, 101, 68, 105, 115, 116, 114, 105, 98, 117, 116, 111, 114,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.base.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.base),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getSetMintInstructionDataEncoder().encode(args as SetMintInstructionDataArgs),
  } as SetMintInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountBase,
    TAccountSystemProgram
  >;

  return instruction;
}

export type SetMintInput<
  TAccountDistributor extends string = string,
  TAccountBase extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  distributor: Address<TAccountDistributor>;
  base: TransactionSigner<TAccountBase>;
  systemProgram?: Address<TAccountSystemProgram>;
  mint: SetMintInstructionDataArgs['mint'];
};

export function getSetMintInstruction<
  TAccountDistributor extends string,
  TAccountBase extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: SetMintInput<TAccountDistributor, TAccountBase, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): SetMintInstruction<TProgramAddress, TAccountDistributor, TAccountBase, TAccountSystemProgram> {
  // Program address.
  const programAddress = config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    distributor: { value: input.distributor ?? null, isWritable: true },
    base: { value: input.base ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.base),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getSetMintInstructionDataEncoder().encode(args as SetMintInstructionDataArgs),
  } as SetMintInstruction<
    TProgramAddress,
    TAccountDistributor,
    TAccountBase,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedSetMintInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    distributor: TAccountMetas[0];
    base: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: SetMintInstructionData;
};

export function parseSetMintInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedSetMintInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      distributor: getNextAccount(),
      base: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetMintInstructionDataDecoder().decode(instruction.data),
  };
}
