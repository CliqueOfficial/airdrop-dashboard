/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS } from '../programs';
import { expectAddress, expectSome, getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_FIXED_FEE_DISCRIMINATOR = new Uint8Array([238, 14, 137, 181, 156, 157, 55, 27]);

export function getSetFixedFeeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_FIXED_FEE_DISCRIMINATOR);
}

export type SetFixedFeeInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountFeeConfig extends string | IAccountMeta<string> = string,
  TAccountDistributor extends string | IAccountMeta<string> = string,
  TAccountBase extends string | IAccountMeta<string> = string,
  TAccountPayer extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends string | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountFeeConfig extends string ? WritableAccount<TAccountFeeConfig> : TAccountFeeConfig,
      TAccountDistributor extends string
        ? ReadonlyAccount<TAccountDistributor>
        : TAccountDistributor,
      TAccountBase extends string
        ? ReadonlySignerAccount<TAccountBase> & IAccountSignerMeta<TAccountBase>
        : TAccountBase,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & IAccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetFixedFeeInstructionData = {
  discriminator: ReadonlyUint8Array;
  configurator: Array<number>;
  fee: bigint;
};

export type SetFixedFeeInstructionDataArgs = {
  configurator: Array<number>;
  fee: number | bigint;
};

export function getSetFixedFeeInstructionDataEncoder(): Encoder<SetFixedFeeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['configurator', getArrayEncoder(getU8Encoder(), { size: 32 })],
      ['fee', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_FIXED_FEE_DISCRIMINATOR })
  );
}

export function getSetFixedFeeInstructionDataDecoder(): Decoder<SetFixedFeeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['configurator', getArrayDecoder(getU8Decoder(), { size: 32 })],
    ['fee', getU64Decoder()],
  ]);
}

export function getSetFixedFeeInstructionDataCodec(): Codec<
  SetFixedFeeInstructionDataArgs,
  SetFixedFeeInstructionData
> {
  return combineCodec(
    getSetFixedFeeInstructionDataEncoder(),
    getSetFixedFeeInstructionDataDecoder()
  );
}

export type SetFixedFeeAsyncInput<
  TAccountFeeConfig extends string = string,
  TAccountDistributor extends string = string,
  TAccountBase extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  feeConfig?: Address<TAccountFeeConfig>;
  distributor?: Address<TAccountDistributor>;
  base: TransactionSigner<TAccountBase>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  configurator: SetFixedFeeInstructionDataArgs['configurator'];
  fee: SetFixedFeeInstructionDataArgs['fee'];
};

export async function getSetFixedFeeInstructionAsync<
  TAccountFeeConfig extends string,
  TAccountDistributor extends string,
  TAccountBase extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: SetFixedFeeAsyncInput<
    TAccountFeeConfig,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SetFixedFeeInstruction<
    TProgramAddress,
    TAccountFeeConfig,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    feeConfig: { value: input.feeConfig ?? null, isWritable: true },
    distributor: { value: input.distributor ?? null, isWritable: false },
    base: { value: input.base ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.distributor.value) {
    accounts.distributor.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            77, 101, 114, 107, 108, 101, 68, 105, 115, 116, 114, 105, 98, 117, 116, 111, 114,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.base.value)),
      ],
    });
  }
  if (!accounts.feeConfig.value) {
    accounts.feeConfig.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([70, 101, 101, 67, 111, 110, 102, 105, 103])),
        getAddressEncoder().encode(expectAddress(accounts.distributor.value)),
        getArrayEncoder(getU8Encoder(), { size: 32 }).encode(expectSome(args.configurator)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.feeConfig),
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.base),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getSetFixedFeeInstructionDataEncoder().encode(args as SetFixedFeeInstructionDataArgs),
  } as SetFixedFeeInstruction<
    TProgramAddress,
    TAccountFeeConfig,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram
  >;

  return instruction;
}

export type SetFixedFeeInput<
  TAccountFeeConfig extends string = string,
  TAccountDistributor extends string = string,
  TAccountBase extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  feeConfig: Address<TAccountFeeConfig>;
  distributor: Address<TAccountDistributor>;
  base: TransactionSigner<TAccountBase>;
  payer: TransactionSigner<TAccountPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  configurator: SetFixedFeeInstructionDataArgs['configurator'];
  fee: SetFixedFeeInstructionDataArgs['fee'];
};

export function getSetFixedFeeInstruction<
  TAccountFeeConfig extends string,
  TAccountDistributor extends string,
  TAccountBase extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: SetFixedFeeInput<
    TAccountFeeConfig,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SetFixedFeeInstruction<
  TProgramAddress,
  TAccountFeeConfig,
  TAccountDistributor,
  TAccountBase,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    feeConfig: { value: input.feeConfig ?? null, isWritable: true },
    distributor: { value: input.distributor ?? null, isWritable: false },
    base: { value: input.base ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.feeConfig),
      getAccountMeta(accounts.distributor),
      getAccountMeta(accounts.base),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getSetFixedFeeInstructionDataEncoder().encode(args as SetFixedFeeInstructionDataArgs),
  } as SetFixedFeeInstruction<
    TProgramAddress,
    TAccountFeeConfig,
    TAccountDistributor,
    TAccountBase,
    TAccountPayer,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedSetFixedFeeInstruction<
  TProgram extends string = typeof MERKLE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    feeConfig: TAccountMetas[0];
    distributor: TAccountMetas[1];
    base: TAccountMetas[2];
    payer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: SetFixedFeeInstructionData;
};

export function parseSetFixedFeeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedSetFixedFeeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      feeConfig: getNextAccount(),
      distributor: getNextAccount(),
      base: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSetFixedFeeInstructionDataDecoder().decode(instruction.data),
  };
}
