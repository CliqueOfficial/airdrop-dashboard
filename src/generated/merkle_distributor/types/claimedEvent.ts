/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
} from '@solana/kit';

/** Emitted when tokens are claimed. */
export type ClaimedEvent = {
  /** User that claimed. */
  claimant: Address;
  /** Amount of tokens to distribute. */
  amount: bigint;
};

export type ClaimedEventArgs = {
  /** User that claimed. */
  claimant: Address;
  /** Amount of tokens to distribute. */
  amount: number | bigint;
};

export function getClaimedEventEncoder(): Encoder<ClaimedEventArgs> {
  return getStructEncoder([
    ['claimant', getAddressEncoder()],
    ['amount', getU64Encoder()],
  ]);
}

export function getClaimedEventDecoder(): Decoder<ClaimedEvent> {
  return getStructDecoder([
    ['claimant', getAddressDecoder()],
    ['amount', getU64Decoder()],
  ]);
}

export function getClaimedEventCodec(): Codec<ClaimedEventArgs, ClaimedEvent> {
  return combineCodec(getClaimedEventEncoder(), getClaimedEventDecoder());
}
