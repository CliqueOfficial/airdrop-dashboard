/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const CLAIM_ROOT_DISCRIMINATOR = new Uint8Array([48, 168, 94, 21, 238, 1, 108, 243]);

export function getClaimRootDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_ROOT_DISCRIMINATOR);
}

export type ClaimRoot = {
  discriminator: ReadonlyUint8Array;
  /** The distributor of the claim root. */
  distributor: Address;
  /** The root of the Merkle tree. */
  root: Array<number>;
  /** The configurator of the Merkle tree. */
  configurator: Array<number>;
};

export type ClaimRootArgs = {
  /** The distributor of the claim root. */
  distributor: Address;
  /** The root of the Merkle tree. */
  root: Array<number>;
  /** The configurator of the Merkle tree. */
  configurator: Array<number>;
};

export function getClaimRootEncoder(): Encoder<ClaimRootArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['distributor', getAddressEncoder()],
      ['root', getArrayEncoder(getU8Encoder(), { size: 32 })],
      ['configurator', getArrayEncoder(getU8Encoder(), { size: 32 })],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_ROOT_DISCRIMINATOR })
  );
}

export function getClaimRootDecoder(): Decoder<ClaimRoot> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['distributor', getAddressDecoder()],
    ['root', getArrayDecoder(getU8Decoder(), { size: 32 })],
    ['configurator', getArrayDecoder(getU8Decoder(), { size: 32 })],
  ]);
}

export function getClaimRootCodec(): Codec<ClaimRootArgs, ClaimRoot> {
  return combineCodec(getClaimRootEncoder(), getClaimRootDecoder());
}

export function decodeClaimRoot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<ClaimRoot, TAddress>;
export function decodeClaimRoot<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<ClaimRoot, TAddress>;
export function decodeClaimRoot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<ClaimRoot, TAddress> | MaybeAccount<ClaimRoot, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getClaimRootDecoder());
}

export async function fetchClaimRoot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<ClaimRoot, TAddress>> {
  const maybeAccount = await fetchMaybeClaimRoot(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeClaimRoot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<ClaimRoot, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeClaimRoot(maybeAccount);
}

export async function fetchAllClaimRoot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<ClaimRoot>[]> {
  const maybeAccounts = await fetchAllMaybeClaimRoot(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeClaimRoot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<ClaimRoot>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeClaimRoot(maybeAccount));
}
